# Phase 1.2.4 — Unified Storage Abstraction (Done)

## Deliverables

- `src/storage/store.py` — `Store` facade over `SQLiteStore` + `ChromaStore`
- `tests/test_store.py` — 12 mocked/unit tests
- `src/storage/__init__.py` — exports `Store`
- `scripts/test_store_live.py` — live smoke test (llama-server on `:8087`)

## Verification (2026-05-28)

```powershell
python -m py_compile src/storage/store.py
$env:PYTHONPATH = (Get-Location).Path
.\.venv\Scripts\python.exe -m pytest tests/test_store.py -v
.\.venv\Scripts\python.exe -m pytest tests/test_sqlite_store.py tests/test_chroma_store.py -v
.\.venv\Scripts\python.exe scripts\test_store_live.py
```

| Suite | Result |
|-------|--------|
| `tests/test_store.py` | **12 passed** |
| `tests/test_sqlite_store.py` + `tests/test_chroma_store.py` | **35 passed** (regression) |

## Live smoke output

```
Health: {'sqlite': True, 'chroma': True, 'chroma_doc_count': 0}
Fact: {'id': 1, 'ticker': 'NVDA', 'metric': 'revenue_q1_2026', 'value': 26.0, ...}
Document saved: test/nvda-earnings

Hybrid search results:
  Ticker detected: None
  Documents found: 1
  Facts found: 0
  [test/nvda-earnings] (dist: 0.344)

After cleanup: {'sqlite': True, 'chroma': True, 'chroma_doc_count': 0}
```

**Note:** Live query uses `"NVIDIA"` (5 letters); `_detect_ticker` only matches `\b[A-Z]{1,4}\b`, so ticker is `None` unless the query includes a symbol like `NVDA`. Unit test `test_detect_ticker_nvda` covers detection; hybrid facts path requires a detected/provided ticker.

## Spec checklist

- [x] `Store.heartbeat()` — both backends healthy
- [x] `save_fundamental` / `get_fundamental` via unified interface
- [x] `save_document` in ChromaDB
- [x] `search()` returns documents (and facts when ticker detected)
- [x] Ticker detection — `search("What is NVDA doing?")` → `NVDA`
- [x] `process_filing()` — document + facts + parsed status
- [x] `reset()` clears both stores
