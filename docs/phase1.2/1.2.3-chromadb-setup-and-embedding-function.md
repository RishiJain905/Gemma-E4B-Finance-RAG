# 1.2.3 — ChromaDB Setup & Embedding Function

## Objective

Initialize ChromaDB with a persistent local database, configure the embedding function to use your TraceAlchemy model's `/v1/embeddings` endpoint, and verify that documents can be stored and retrieved via semantic search.

## Why This Matters

ChromaDB is the **vector** half of the hybrid storage layer. It handles semantic search — finding documents by meaning rather than by keyword. When you ask "what's the market sentiment around NVIDIA's Blackwell launch?", ChromaDB finds the relevant news articles, earnings call excerpts, and analyst notes, even if none of them mention the exact words "market sentiment."

The embedding function is the critical piece: ChromaDB calls `http://127.0.0.1:8087/v1/embeddings` every time you add or search documents. If this connection is wrong, nothing works.

## How ChromaDB + llama-server Interact

```
You add: "NVIDIA datacenter revenue grew 42% YoY..."
  → ChromaDB calls: POST /v1/embeddings {"input": "..."}
  → llama-server returns: [0.012, -0.045, 0.078, ...] (2048 floats)
  → ChromaDB stores: vector + metadata in data/chroma/

You search: "What's happening with datacenter revenue?"
  → ChromaDB calls: POST /v1/embeddings {"input": "..."}
  → llama-server returns: vector for your query
  → ChromaDB finds: nearest vectors in its index
  → Returns: the documents with matching metadata
```

## Steps

### 1. Create the ChromaDB Module

Create `src/storage/chroma_store.py`:

```python
"""
src/storage/chroma_store.py
ChromaDB vector store for document embeddings.
"""

from pathlib import Path
from typing import Optional
import httpx
import numpy as np
import chromadb
from chromadb import Documents, EmbeddingFunction, Embeddings


class TraceAlchemyEmbeddingFunction(EmbeddingFunction):
    """
    Custom embedding function that calls your local llama-server
    /v1/embeddings endpoint with --pooling mean enabled.
    """

    def __init__(self, endpoint: str = "http://127.0.0.1:8087/v1/embeddings",
                 model: str = "tracealchemy",
                 batch_size: int = 10,
                 timeout: int = 60):
        self.endpoint = endpoint
        self.model = model
        self.batch_size = batch_size
        self.timeout = timeout
        self._client = httpx.Client(timeout=timeout)

    def __call__(self, input: Documents) -> Embeddings:
        """
        ChromaDB calls this with a list of text documents.
        Returns a list of embedding vectors (list of floats).
        """
        # Process in batches to avoid overloading the server
        all_embeddings = []

        for i in range(0, len(input), self.batch_size):
            batch = input[i:i + self.batch_size]

            payload = {
                "input": batch if len(batch) > 1 else batch[0],
                "model": self.model,
            }

            response = self._client.post(self.endpoint, json=payload)
            response.raise_for_status()

            data = response.json()

            # llama-server returns: {"data": [{"embedding": [...], ...}, ...]}
            for item in data["data"]:
                all_embeddings.append(item["embedding"])

        return all_embeddings

    def __del__(self):
        if hasattr(self, "_client"):
            self._client.close()


class ChromaStore:
    """
    Wraps ChromaDB collection operations.
    Provides add_document, search, and delete methods.
    """

    DEFAULT_PATH = Path(__file__).parent.parent.parent / "data/chroma"

    def __init__(self,
                 persist_directory: Optional[Path] = None,
                 collection_name: str = "tracealchemy_docs",
                 embedding_endpoint: str = "http://127.0.0.1:8087/v1/embeddings"):

        self.persist_directory = persist_directory or self.DEFAULT_PATH
        self.persist_directory.mkdir(parents=True, exist_ok=True)

        # Create embedding function
        self.embedding_fn = TraceAlchemyEmbeddingFunction(
            endpoint=embedding_endpoint
        )

        # Initialize ChromaDB with persistent storage
        self.client = chromadb.PersistentClient(
            path=str(self.persist_directory)
        )

        # Get or create the collection
        self.collection = self.client.get_or_create_collection(
            name=collection_name,
            embedding_function=self.embedding_fn,
            metadata={"hnsw:space": "cosine"}  # Cosine similarity for finance
        )

    # ── CRUD Operations ──────────────────────────────

    def add_document(self,
                     document_id: str,
                     text: str,
                     metadata: dict = None,
                     ticker: str = None,
                     source: str = None,
                     date: str = None):
        """
        Add a single document to the vector store.

        Args:
            document_id: Unique identifier (e.g., 'filings/NVDA/10-Q-2026-Q1')
            text: The document content to embed and store
            metadata: Additional metadata dict
            ticker: Stock ticker (added to metadata)
            source: Source type (sec, yfinance, gdelt, etc.)
            date: Document date (ISO format)
        """
        meta = metadata or {}
        if ticker:
            meta["ticker"] = ticker.upper()
        if source:
            meta["source"] = source
        if date:
            meta["date"] = date

        self.collection.add(
            documents=[text],
            metadatas=[meta],
            ids=[document_id]
        )

    def add_documents_batch(self,
                            ids: list[str],
                            texts: list[str],
                            metadatas: list[dict] = None):
        """
        Add multiple documents at once (more efficient).

        Args:
            ids: List of unique document IDs
            texts: List of document texts
            metadatas: List of metadata dicts (same length as ids)
        """
        self.collection.add(
            documents=texts,
            metadatas=metadatas or [{}] * len(ids),
            ids=ids
        )

    def search(self, query: str, n_results: int = 5,
               filter_dict: dict = None) -> list[dict]:
        """
        Search for documents semantically similar to the query.

        Args:
            query: Natural language query string
            n_results: Number of results to return
            filter_dict: Optional metadata filter (e.g., {"ticker": "NVDA"})

        Returns:
            List of dicts with: id, document, metadata, distance
        """
        results = self.collection.query(
            query_texts=[query],
            n_results=n_results,
            where=filter_dict
        )

        # Format results into a cleaner structure
        formatted = []
        if results["ids"] and results["ids"][0]:
            for i in range(len(results["ids"][0])):
                formatted.append({
                    "id": results["ids"][0][i],
                    "document": results["documents"][0][i],
                    "metadata": results["metadatas"][0][i],
                    "distance": results["distances"][0][i] if results.get("distances") else None,
                })

        return formatted

    def get_document(self, document_id: str) -> Optional[dict]:
        """Retrieve a document by its ID."""
        results = self.collection.get(ids=[document_id])
        if results["ids"]:
            return {
                "id": results["ids"][0],
                "document": results["documents"][0] if results["documents"] else None,
                "metadata": results["metadatas"][0] if results["metadatas"] else None,
            }
        return None

    def delete_document(self, document_id: str):
        """Remove a document from the collection."""
        self.collection.delete(ids=[document_id])

    def count(self) -> int:
        """How many documents are in the collection?"""
        return self.collection.count()

    # ── Ticker-Specific Operations ────────────────────

    def search_by_ticker(self, query: str, ticker: str,
                         n_results: int = 5) -> list[dict]:
        """Search only documents related to a specific ticker."""
        return self.search(
            query=query,
            n_results=n_results,
            filter_dict={"ticker": ticker.upper()}
        )

    def get_ticker_documents(self, ticker: str,
                             source: str = None,
                             limit: int = 20) -> list[dict]:
        """Get all documents for a ticker, optionally filtered by source."""
        where = {"ticker": ticker.upper()}
        if source:
            where["source"] = source

        results = self.collection.get(where=where, limit=limit)
        formatted = []
        if results["ids"]:
            for i in range(len(results["ids"])):
                formatted.append({
                    "id": results["ids"][i],
                    "document": results["documents"][i] if results["documents"] else None,
                    "metadata": results["metadatas"][i] if results["metadatas"] else None,
                })
        return formatted

    # ── Collection Management ─────────────────────────

    def reset_collection(self):
        """Drop and recreate the collection (for testing)."""
        self.client.delete_collection(self.collection.name)
        self.collection = self.client.create_collection(
            name=self.collection.name,
            embedding_function=self.embedding_fn,
            metadata={"hnsw:space": "cosine"}
        )

    def heartbeat(self) -> bool:
        """Check if ChromaDB is responsive."""
        try:
            self.client.heartbeat()
            return True
        except Exception:
            return False
```

### 2. Verify the Embedding Dimension

Before using ChromaDB, confirm the embedding dimension from Task 1.1.7:

```powershell
# From project root with .venv active
python -c "
import httpx
r = httpx.post('http://127.0.0.1:8087/v1/embeddings',
    json={'input': 'Verify embedding dimension', 'model': 'tracealchemy'})
dim = len(r.json()['data'][0]['embedding'])
print(f'Embedding dimension: {dim}')
"
```

Update `configs/storage.yaml`:
```yaml
chroma:
  embedding_dimension: 2048  # ← Replace with actual value from above
```

### 3. Test the Full Pipeline

```powershell
# From project root with .venv active and llama-server running
python -c "
from src.storage.chroma_store import ChromaStore

store = ChromaStore()

# Add test documents
store.add_document(
    document_id='test/nvda-001',
    text='NVIDIA reported record datacenter revenue of $26 billion in Q1 2026, driven by demand for Blackwell GPUs.',
    ticker='NVDA',
    source='sec_filing',
    date='2026-05-15'
)

store.add_document(
    document_id='test/nvda-002',
    text='AMD announced the MI400 AI accelerator, positioning it as a direct competitor to NVIDIA Blackwell.',
    ticker='AMD',
    source='news',
    date='2026-05-14'
)

store.add_document(
    document_id='test/macro-001',
    text='The Federal Reserve kept interest rates unchanged at 4.5%, citing persistent inflation concerns.',
    ticker=None,
    source='fred',
    date='2026-05-10'
)

print(f'Documents stored: {store.count()}')

# Semantic search
results = store.search('What is NVIDIA doing in the data center space?')
print(f'\nSearch results for \"data center\":')
for r in results:
    print(f'  [{r[\"id\"]}] (dist: {r[\"distance\"]:.3f}) {r[\"document\"][:80]}...')

# Ticker-filtered search
nvda_results = store.search_by_ticker('What is happening?', 'NVDA')
print(f'\nTicker-filtered results for NVDA: {len(nvda_results)}')
for r in nvda_results:
    print(f'  [{r[\"id\"]}] {r[\"document\"][:80]}...')

# Clean up test data
store.delete_document('test/nvda-001')
store.delete_document('test/nvda-002')
store.delete_document('test/macro-001')
print(f'\nAfter cleanup: {store.count()} documents')
"
```

**Expected behavior:**
- `Documents stored: 3`
- Search for "data center" returns NVDA doc as highest match (lowest distance)
- Ticker-filtered search for NVDA returns only the NVDA doc
- After cleanup: `0 documents`

### 4. Check the Cosine Similarity Quality

The `search()` results include a `distance` field. With cosine similarity:

| Distance | Meaning |
|----------|---------|
| 0.0 - 0.3 | Very similar — highly relevant |
| 0.3 - 0.6 | Moderately similar — probably relevant |
| 0.6 - 0.8 | Somewhat related — tangentially relevant |
| 0.8 - 1.0 | Not very similar — weak match |
| > 1.0 | Dissimilar — likely irrelevant |

If all distances cluster tightly (e.g., 0.95-1.0), the embeddings may not be discriminating well. This is rare with Q8_0 quant but worth noting.

## What Could Go Wrong

| Problem | Likely Cause | Fix |
|---------|-------------|------|
| `chromadb.errors.NoEmbeddingFunctionException` | Embedding function not configured | Pass `embedding_function` to `get_or_create_collection` |
| `httpx.ConnectError` at embed endpoint | llama-server not running or wrong port | Start server on 8087, verify with `curl` |
| `ValueError: embedding dimension 0 does not match collection dimension 2048` | Wrong dimension configured | Update `configs/storage.yaml` with correct dimension from Task 1.1.7 |
| `chromadb.errors.InvalidCollectionException` | Collection was reset or deleted | `get_or_create_collection` handles this — just re-run |
| Documents added but search returns empty | Embedding function returning empty vectors | Test the embedding function standalone first |
| Search returns documents but distances are all ~1.0 | Embeddings aren't semantically meaningful | Try different `-ctk`/`-ctv` values or check that `--pooling mean` is active |
| `sqlite3.OperationalError: database is locked` | ChromaDB uses SQLite internally | Ensure no other process has ChromaDB open. WAL mode helps. |

## Verification

- [ ] `TraceAlchemyEmbeddingFunction` successfully calls `llama-server` and returns vectors
- [ ] `ChromaStore` initializes without errors with a persistent database
- [ ] Documents can be added, searched, and deleted
- [ ] Search results are ranked by relevance (lower distance = more relevant)
- [ ] Ticker-filtered search returns only documents for that ticker
- [ ] `store.count()` returns the correct number of documents
- [ ] Cosine similarity distances are meaningful (not all clustered near 1.0)
- [ ] Embedding dimension matches across storage.yaml and actual endpoint

## Next Step

Once ChromaDB is working, proceed to **1.2.4-unified-storage-abstraction.md**.
