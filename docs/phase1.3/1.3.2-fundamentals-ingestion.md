# 1.3.2 — Fundamentals Ingestion

## Objective

Implement the fundamentals ingestion pipeline — pull real financial metrics from Yahoo Finance (revenue, EPS, PE ratio, market cap, gross margins, etc.) and store them in SQLite via `store.save_fundamental()`.

## Why This Matters

The manually seeded fundamentals from Phase 1.2 will go stale. This module makes them **self-updating** — every 24 hours (or whatever schedule you configure), it re-fetches the latest data and keeps your SQLite store current.

Importantly, this module uses **cache awareness** — it checks if a ticker's fundamentals are still fresh before hitting Yahoo Finance. No unnecessary API calls.

## Step 1: Update the YFinanceIngestor

Extend `src/ingestion/yfinance_ingestor.py` with fundamentals ingestion logic. Add this after the existing stubs:

```python
    # ── Fundamentals Ingestion ────────────────────────

    FUNDAMENTAL_METRICS = [
        # (metric_name, info_key, unit, period_type)
        ("market_cap",        "marketCap",         "usd",      "point_in_time"),
        ("pe_ratio_ttm",      "trailingPE",        "ratio",    "ttm"),
        ("forward_pe",        "forwardPE",         "ratio",    "forward"),
        ("eps_ttm",           "trailingEps",       "usd",      "ttm"),
        ("dividend_yield",    "dividendYield",     "percent",  "ttm"),
        ("price_to_book",     "priceToBook",       "ratio",    "ttm"),
        ("debt_to_equity",    "debtToEquity",      "ratio",    "ttm"),
        ("revenue_ttm",       "totalRevenue",      "usd",      "ttm"),
        ("gross_margin_ttm",  "grossMargins",      "percent",  "ttm"),
        ("operating_margin",  "operatingMargins",  "percent",  "ttm"),
        ("profit_margin",     "profitMargins",     "percent",  "ttm"),
        ("revenue_growth",    "revenueGrowth",     "percent",  "yoy"),
        ("earnings_growth",   "earningsGrowth",    "percent",  "yoy"),
        ("return_on_equity",  "returnOnEquity",    "percent",  "ttm"),
        ("free_cash_flow",    "freeCashflow",      "usd",      "ttm"),
        ("operating_cf",      "operatingCashflow",  "usd",     "ttm"),
        ("current_ratio",     "currentRatio",      "ratio",    "ttm"),
        ("quick_ratio",       "quickRatio",        "ratio",    "ttm"),
    ]

    def ingest_fundamentals(self):
        """Ingest fundamentals for all core tickers."""
        logger.info("Ingesting fundamentals for %d core tickers...", len(self.core_tickers))
        success_count = 0
        skip_count = 0

        for ticker in self.core_tickers:
            # Cache check — skip if fundamentals are still fresh
            if self._fundamentals_fresh(ticker):
                logger.debug("Ticker %s fundamentals are fresh, skipping", ticker)
                skip_count += 1
                continue

            t = self._fetch_ticker(ticker)
            if t is None:
                continue

            self._ingest_ticker_fundamentals(ticker, t)
            success_count += 1

        logger.info(
            "Fundamentals ingestion complete: %d ingested, %d skipped (fresh)",
            success_count, skip_count
        )

    def _fundamentals_fresh(self, ticker: str) -> bool:
        """Check if fundamentals cache is still fresh for this ticker."""
        ttl_hours = self.watchlist.get("schedule", {}).get("fundamentals", 24)
        # Use the cache_meta table via sqlite store
        status = self.store.sqlite.get_cache_status(ticker, "yfinance_fundamentals")
        if status and status.get("status") == "fresh":
            # Also verify it isn't expired by checking the stored TTL
            from datetime import datetime, timezone
            accessed = status.get("accessed_at")
            if accessed:
                age_hours = (datetime.now(timezone.utc) - accessed).total_seconds() / 3600
                return age_hours < ttl_hours
        return False

    def _ingest_ticker_fundamentals(self, ticker: str, t):
        """Ingest fundamentals for a single ticker."""
        info = t.info
        period_label = self._current_period_label()
        saved = 0

        for metric_name, info_key, unit, period_type in self.FUNDAMENTAL_METRICS:
            raw_value = info.get(info_key)
            if raw_value is None:
                logger.debug("Ticker %s has no %s (%s), skipping", ticker, metric_name, info_key)
                continue

            # Normalize the value
            value = self._normalize_value(raw_value)
            if value is None:
                continue

            self.store.save_fundamental(
                ticker=ticker,
                metric=metric_name,
                value=value,
                unit=unit,
                period=period_label,
                period_type=period_type,
                source_type="yfinance",
            )
            saved += 1

        # Mark cache as fresh
        ttl_hours = self.watchlist.get("schedule", {}).get("fundamentals", 24)
        self.store.mark_cache_fresh(ticker, "yfinance_fundamentals", ttl_hours)
        logger.info("Ticker %s: saved %d fundamentals, cache marked fresh (%dh)", ticker, saved, ttl_hours)

    def _normalize_value(self, raw_value):
        """Normalize a raw Yahoo Finance value for storage."""
        if raw_value is None:
            return None
        try:
            return float(raw_value)
        except (TypeError, ValueError):
            return None

    def _current_period_label(self) -> str:
        """Generate a period label like '2026-Q1' for the current quarter."""
        from datetime import datetime
        now = datetime.now()
        quarter = (now.month - 1) // 3 + 1
        return f"{now.year}-Q{quarter}"
```

## Step 2: Run a Single Ticker Test

Test that fundamentals ingestion works for one ticker:

```powershell
# From project root with llama-server running
python -c "
from src.ingestion.yfinance_ingestor import YFinanceIngestor

ingestor = YFinanceIngestor()

# Test NVDA fundamentals
t = ingestor._fetch_ticker('NVDA')
if t:
    ingestor._ingest_ticker_fundamentals('NVDA', t)
    print('NVDA fundamentals ingested')

# Verify it was stored
from src.storage.store import Store
store = Store()
facts = store.get_fundamentals_batch('NVDA')
for metric, fact in facts.items():
    print(f'  {metric}: {fact[\"value\"]} {fact[\"unit\"]}')
"
```

**Expected output:** A list of ~18 fundamentals for NVDA with real values.

## Step 3: Run Fundamentals for All Core Tickers

```powershell
python -c "
from src.ingestion.yfinance_ingestor import YFinanceIngestor

ingestor = YFinanceIngestor()
ingestor.ingest_fundamentals()
"
```

## Step 4: Verify Cache Awareness

Run the same command again — it should skip all tickers since they're now fresh:

```powershell
python -c "
from src.ingestion.yfinance_ingestor import YFinanceIngestor

ingestor = YFinanceIngestor()
ingestor.ingest_fundamentals()
"
```

**Expected output:** All tickers show as "skipped (fresh)" — no actual fetch happens.

## What Gets Stored

For each core ticker, these metrics are stored in SQLite:

| Metric | Source Key | Unit | Example (NVDA) |
|--------|-----------|------|-----------------|
| Market cap | `marketCap` | usd | $3.2T |
| P/E ratio (TTM) | `trailingPE` | ratio | 38.5 |
| Forward P/E | `forwardPE` | ratio | 35.2 |
| EPS (TTM) | `trailingEps` | usd | $2.84 |
| Dividend yield | `dividendYield` | percent | 0.03% |
| Total revenue (TTM) | `totalRevenue` | usd | $130.5B |
| Gross margin | `grossMargins` | percent | 74.5% |
| Operating margin | `operatingMargins` | percent | 62.1% |
| Profit margin | `profitMargins` | percent | 55.3% |
| Revenue growth (YoY) | `revenueGrowth` | percent | 90.2% |
| Earnings growth | `earningsGrowth` | percent | 120.4% |
| Free cash flow | `freeCashflow` | usd | $62.1B |
| Current ratio | `currentRatio` | ratio | 3.8 |
| Debt-to-equity | `debtToEquity` | ratio | 0.45 |

## What Could Go Wrong

| Problem | Likely Cause | Fix |
|---------|-------------|------|
| `info` field is empty or sparse | Ticker may be delisted or not traded | Skip and log warning |
| `KeyError` on info key | Yahoo changed their API shape | Check the key exists with `info.keys()` |
| Values are `None` | Ticker doesn't report that metric | Skip gracefully (already handled) |
| All tickers "skipped (fresh)" on first run | Cache collision from seed data | Run with `ingest_all()` or wait for TTL expiry |
| Cache status returns `None` for a ticker | Not yet cached | `_fundamentals_fresh()` returns False, so it fetches correctly |
| `datetime.now(timezone.utc)` is naive | Python version < 3.11 zoneinfo issue | Use `datetime.utcnow()` fallback if needed |

## Verification

- [ ] `yfinance` installed and Ticker objects return real data
- [ ] `_ingest_ticker_fundamentals()` saves ~18 metrics to SQLite
- [ ] `store.get_fundamentals_batch()` returns real values (not None)
- [ ] Cache awareness works — re-running skips fresh tickers
- [ ] Cache freshness respects the configured TTL from watchlist.yaml
- [ ] Single ticker test succeeds before running all

## Next Step

Once fundamentals ingestion is working, proceed to **1.3.3-news-and-summary-ingestion.md**.
