# Phase 2.2 — 2.2.1 through 2.2.3 build loop

Branch: `phase-2.2.1-2.2.3` (from `Rishi-Ghost`)

Covers the first three feature groups of the Phase 2.2 roadmap
([README-2.2.md](README-2.2.md)): baseline correctness/evaluation fidelity,
conversational query understanding, and adaptive RAG orchestration. 2.2.1 must
land first — no later metric in 2.2.2/2.2.3 is trustworthy until the
document/prompt evidence contract and evaluator fidelity are repaired.

## 2.2.1 — Evaluation fidelity and baseline correctness

- [x] 2.2.1.1 retrieved-document-contract-and-prompt-policy — spec: `docs/phase2.2/2.2.1-evaluation-fidelity-and-baseline-correctness/2.2.1.1-retrieved-document-contract-and-prompt-policy.md` — fix the `document` vs `text` blank-body bug (P0 evidence loss), establish one shared evidence contract and one authoritative prompt-policy builder. — **Done:** `evidence.py` + `prompt_policy.py` added; grounding/counts use usable evidence; per-fact provenance preserved; all model paths share one policy builder; also fixed collateral `truncate_if_needed` split bug and multi-ticker fact relabeling; VERIFY: PASS (794 passed).
- [x] 2.2.1.2 exact-evidence-trace-and-policy-aware-scoring — spec: `docs/phase2.2/2.2.1-evaluation-fidelity-and-baseline-correctness/2.2.1.2-exact-evidence-trace-and-policy-aware-scoring.md` — capture the exact model-visible prompt/evidence/tool results per answer; replace the truncated re-retrieval judge pass; unblocks the 2.1.7 faithfulness gate. — **Done:** opt-in `EvidenceTrace` (schema v1) captured request-scoped on all answer paths incl. tool loop + streaming; live runner consumes the trace (second retrieval removed); grounded_faithfulness/policy_compliance split with denominators; policy-specific baselines + pretrace gate checks. Caveat: baselines are placeholders until a live scored run (re-baseline after 2.2.1.3 stabilizes the golden set). VERIFY: PASS (809 passed).
- [x] 2.2.1.3 conversational-and-compound-query-golden-set — spec: `docs/phase2.2/2.2.1-evaluation-fidelity-and-baseline-correctness/2.2.1.3-conversational-and-compound-query-golden-set.md` — extend golden set/runner with versioned fixtures for multi-turn, compound, multi-ticker, stale, and unanswerable cases. — **Done:** 13 conversations/31 turns + 38 new single-turn cases (85 total), runner-owned history with forward-compatible `history` field, 8 deterministic conversational metrics, Phase 2.2 acceptance thresholds in gate (fail on zero eligible fixtures); feature RESULTS.md written. VERIFY: PASS (840 passed).

## 2.2.2 — Conversational query understanding

- [x] 2.2.2.1 request-history-contract-and-bounded-memory — spec: `docs/phase2.2/2.2.2-conversational-query-understanding/2.2.2.1-request-history-contract-and-bounded-memory.md` — add a bounded, client-owned `ChatTurn` history contract to `/query`; middleware stays stateless. — **Done:** ChatTurn + bounded select_history (8 turns/8k chars), question cap 2k→16k with explicit 422 (no silent truncation), ChatSession owns history + /new /clear /history controls, eval runner sends ChatTurn shape; single-turn behavior byte-for-byte unchanged. VERIFY: PASS (856 passed).
- [x] 2.2.2.2 follow-up-rewriting-and-entity-carryover — spec: `docs/phase2.2/2.2.2-conversational-query-understanding/2.2.2.2-follow-up-rewriting-and-entity-carryover.md` — compile turn + history into a standalone retrieval query; deterministic ticker/metric/period carryover with at most one optional planner call. — **Done:** ConversationState + CompiledQuestion + compile_question with conservative carryover rules; retrieval uses compiled query, raw question untouched; optional schema/catalog-validated LLM fallback (off by default, zero calls on deterministic turns); topic_reset populated; resolved slots exposed to eval. All behind enable_conversation_rewrite (default off). VERIFY: PASS (878 passed).
- [x] 2.2.2.3 multiline-chat-sessions-and-evaluation — spec: `docs/phase2.2/2.2.2-conversational-query-understanding/2.2.2.3-multiline-chat-sessions-and-evaluation.md` — `/ask` multiline composer + session controls in `scripts/chat.py`; offline integration test for verbose/follow-up questions. — **Done:** /ask composer (/send /cancel /preview, no new deps), /health capability flags with old-server fallback, full 422 detail, streaming disabled only on 404/405, session·turns prompt suffix + verbose carried-context, /eval conversations [N]. VERIFY: PASS (869 passed; 21 skips are env-conditional — :8087 was down during the run, unrelated to this change).

## 2.2.3 — Adaptive RAG orchestration

- [x] 2.2.3.1 multi-entity-intent-and-query-plans — spec: `docs/phase2.2/2.2.3-adaptive-rag-orchestration/2.2.3.1-multi-entity-intent-and-query-plans.md` — explicit `QueryPlan` contract (entities, intents, metrics, periods) behind a feature flag; `IntentParser.parse()` kept as compatibility view. — **Done:** query_plan.py contracts + validate(), resolve_all span-aware multi-entity resolution (resolve() untouched), parse_plan multi-intent/metric/period, to_legacy_intent byte-compatible; micro-F1 pooled 0.965 (entities 1.00, periods 1.00) on 16-query fixture set; also fixed offline-gate leak (unmarked live filing e2e test). VERIFY: PASS (922 passed).
- [x] 2.2.3.2 deterministic-finance-tool-routing — spec: `docs/phase2.2/2.2.3-adaptive-rag-orchestration/2.2.3.2-deterministic-finance-tool-routing.md` — rule-based router for safe analytical/comparison/projection/calculation requests; model no longer decides whether to invoke exact DB operations. — **Done:** pure route(plan) + read-only executor (≤3 tools, fail-soft), unified dispatch_named_tool (single validation path), Decimal-only whitelisted calculator with provenance; 100% routing accuracy on 21-case matrix, 100% write avoidance, p95 0.007 ms; flags default off. VERIFY: PASS (939 passed).
- [x] 2.2.3.3 adaptive-lanes-context-budget-and-conditional-reranking — spec: `docs/phase2.2/2.2.3-adaptive-rag-orchestration/2.2.3.3-adaptive-lanes-context-budget-and-conditional-reranking.md` — bounded orchestrator selecting fast/standard/complex lanes under one shared execution budget (≤3 subqueries, ≤2 retrieval rounds, ≤1 planner call, ≤1 re-rank call). — **Done:** adaptive_orchestrator.py with uncircumventable ExecutionBudget, single ContextBudget owner (4k/12k/18k lane caps, coverage-ordered selection), conditional re-rank on ambiguity signals only, retrieve_candidates() seam (retrieve() byte-identical), never-raises fallback; orchestration overhead p95 ≤2.5 ms offline; flags default off. Note: survived a mid-task usage-limit interruption and resumed cleanly. VERIFY: PASS (953 passed).
- [x] 2.2.3.4 integration-and-evaluation — spec: `docs/phase2.2/2.2.3-adaptive-rag-orchestration/2.2.3.4-integration-and-evaluation.md` — wire query plan + deterministic router + adaptive orchestrator into `/query` and `/query/stream` behind `enable_adaptive_rag`; promotion checkpoint comparing quality/latency vs. the legacy path. — **Done:** single request-path switch with adaptive_fallback to legacy, actual-counter orchestration metadata + trace extensions, three-config eval scaffolding (--config-label) with documented live commands, promotion safety checks in gate; also fixed 14/16 findings from two independent external reviews (gpt-5.5 + gpt-5.6-luna) incl. fast-lane obligation guard, plan-coverage completeness, fail-soft boundary, retriever concurrency; flag default off, legacy path is the rollback. Live three-config comparison pending GPU session. VERIFY: PASS (993 passed).

## Rules

- Gate: `scripts\verify.ps1` → `VERIFY: PASS`.
- 2.2.1 blocks 2.2.2/2.2.3 metrics — land and verify all of 2.2.1 before trusting evaluation output from later tasks, per `README-2.2.md`'s suggested build order.
- Two failed gate attempts on the same failure for one task → mark **BLOCKED** below with a one-line diagnosis, move to the next task. Don't loop on it.
- One commit per task, checked off here with a one-line result note as you go.
- Each implemented feature writes its own `RESULTS.md` inside the feature folder (setup, before/after metrics, caveats, regression-gate outcome, ship/rollback decision) — not created speculatively ahead of implementation.
- No push, no merge to `Rishi-Ghost` — leave the branch for review.
